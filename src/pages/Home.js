import React from "react";
import Uploader from "../components/Uploader";
import UploadedDisplay from "../components/UploadedDisplay";
const Home = () => {
  return (
    <div>
      <Uploader />
      <UploadedDisplay />
    </div>
  );
};

export default Home;
